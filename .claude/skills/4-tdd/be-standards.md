# Backend Standards — rr-dev

This project has no backend and none is planned — see CLAUDE.md's Stack
Declaration. There is nothing here to read for actual work on this repo.

This file exists only for skill-set consistency with sibling projects
(researchpulse, vite-react-ts-starter) that do have or may grow a
backend — so the same file names/shapes exist across all three repos,
even where a given file is inert.
